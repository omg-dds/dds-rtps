# RustDDS interoperability test application

## Installation

* Install [Rust development tools](https://www.rust-lang.org/tools/install)
* Build the application in the normal way, using  `cargo build`
* Run using cargo, e.g.
  ** `cargo run -- --help` or
  ** `cargo run -- -P -t Triangle`

